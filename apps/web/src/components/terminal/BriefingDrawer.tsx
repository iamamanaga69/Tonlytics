'use client';

/**
 * Article modals were removed from the reading experience.
 * Briefings now open as full pages at /briefing/[slug].
 */
export default function BriefingDrawer() {
  return null;
}
